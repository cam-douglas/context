# 2026-08-30 continuation

- Shipped **Context 5** (`Ctx5`, `com.context.audio5`). A VST cannot import clips into Ableton. The device keeps a waveform; drag it into Live. Permanent WAV/MIDI: `~/Library/Application Support/Context/Plugin`. Audition temps in `Plugin/.audition` delete on Stop unless Apply. Drop a reference audio file onto the plugin for the next generation.
- Owner: Amount and Wet removed. Reverence/Abstraction change notes and timbre.
- Owner: do not use a stand-in for the rotated package. Installed `diffusers` 0.40.0 + `accelerate` + `einops` + `torchsde`. Wired `StableAudioPipeline` to `stabilityai/stable-audio-open-1.0`. After license accept, downloaded 15.6 GB weights. Smoke compose: 705644-byte 44.1 kHz stereo WAV, RMS 4583, backend `stable-audio-open-1.0`, 35s wall. DDIM scheduler (default SDE hit recursion on MPS). Plugin compose timeout 600s. Blocker moved to `blockers-fixed/stable-audio-open-gated.md`.
- Owner: use mido and Magenta as well. Installed `note-seq` 0.0.5. Compose quantizes via Magenta NoteSequence, then writes `.mid` with mido (pretty_midi fallback). Did not install TensorFlow `magenta` 2.1.4 (would fight torch/diffusers). Tests: `tests.test_midi_io`, `tests.test_stack`, `tests.test_compose`, `tests.test_generation_rotate` OK.
- Owner: wire pyroomacoustics so it is used. Compose now convolves every written WAV with a ShoeBox IR (`apply_room_to_wav`). Abstraction/reverence set room size and wet. Pedalboard reverb was reduced so the IR is the room. Tests: `tests.test_dsp`, `tests.test_compose`, `tests.test_midi_io`, `tests.test_stack` OK. Sidecar restarted. Room is skipped when the active generator writes no WAV (current Stable Audio Open gate).
- Owner: wire Stable Audio Open and Magenta MelodyRNN/MusicVAE together. Split: SAO stays WAV in torch sidecar; MelodyRNN/MusicVAE run in `sidecar/.venv-magenta` and only generate notes. Compose uses Magenta notes when the worker+checkpoints work, else `notes_for` with an honest backend. Tests: `tests.test_magenta_models` plus compose/midi/stack OK. Setup script: `sidecar/scripts/setup-magenta.sh`.
- Owner: finish Magenta setup. Official magenta 2.1.4 pins do not resolve on Py3.11; installed TF 2.21 + magenta --no-deps + tf-keras/tfp/tfds. Checkpoints in `~/Library/Application Support/Context/models/magenta`: attention_rnn.mag, cat-mel_2bar_big.tar, cat-drums_2bar_small.lokl.tar. Verified: melody_rnn 15 notes, music_vae drums 60 notes, compose backends.notes=melody_rnn. Sidecar restarted.
- Owner: wire Demucs, MusicGen, Stable Audio Open, and CLAP. MusicGen/SAO were already the rotate generate path; `/synthesize` now calls them when `CONTEXT_ENABLE_GENERATION=1`. Demucs 4.1.0 installed; compose/`/stems` write drums/bass/vocals/other when `CONTEXT_ENABLE_DEMUCS=1`. CLAP is transformers `laion/clap-htsat-unfused` (not `laion_clap`); `/search` and compose sample-library lookup use it when `CONTEXT_ENABLE_CLAP=1`. Default library: `~/Library/Application Support/Context/Samples`. `run-sidecar.sh` turns the flags on. Tests: `tests.test_heavy_adapters` plus compose/phase2/stack. Phase 10 plan: `docs/plans/phase_10_demucs_clap_generation_plan.md`.
- Owner: no genre-special Magenta prompts. "House prompt" / "melody prompt" were agent smoke-test strings, not product presets. The 15 notes were MelodyRNN output (not saved) from an A-minor primer. Removed house/drums vs melody model switch. Every prompt now: MusicVAE melody seeds, MelodyRNN extends. Tests: `tests.test_magenta_models` OK.
- Owner: wire als-json and DawDreamer for DAW session export with integrity. Installed dawdreamer 0.9.0. Lossless ALS↔JSON codec, additive merge into a cloned set (Live DefaultLiveSet or user `source_als`), never overwrite source. Compose writes `*.als` + `*.als.json`. `/export` also DawDreamer-renders in a subprocess. 46 tests OK. Decision: `docs/decisions/2026-08-30-als-json-session-export.md`. Live File → Open is the remaining owner smoke.
- Owner: in-plugin sample library. **Library** opens a Finder-style table. Choose a folder (persisted in plugin state). All lists every wav/aiff/flac/mp3/ogg. Search sends the prompt to sidecar `/search` (CLAP when enabled, else filename). Double-click or Use as reference; drag a row into Live; Reveal in Finder. Empty `/search` query browses. Compose also uses `library_folder`. Plugin rebuilt. Tests: search browse + heavy adapters OK.
- Owner: write the generation system prompt into the plugin in one place, with backdoors for system / negative / rules and a visible hierarchy. Canonical defaults: `plugin/src/PromptPolicy.h`. Hidden Prompt Gate: Option-click or 5-click **Context 5**. Face line is REQUEST (suggestion). SYSTEM and RULES are hard; NEGATIVE is a hard reject. Sidecar `prompt_policy.py` assembles the same ranks for MusicGen and Stable Audio Open. Tests: `tests.test_prompt_policy` + `tests.test_genres` 8 OK. Plugin rebuilt; sidecar restarted.
- Owner: `invalid HTTP response` during generation. Health timer + `launchctl kickstart -k` was killing the sidecar mid-SAO; `/intent` also composed on an 8s timeout. Cheap `/health`, no kill if 8765 is listening, no compose-on-intent, JSON 500 on compose exceptions, no local stand-in on sidecar errors. Demucs no longer runs after every generate (`POST /stems` still works). Tests: http/compose/rotate/phase2-6 OK. Context 5 rebuilt. Sidecar restarted; first generate will reload weights.
- Owner: rescanned and still did not see Library. Live 11 (PID was running) keeps the loaded AU binary; rescan only updates the catalog. Shipped **Context 6** (`Ctx6`, `com.context.audio6`) with Library next to Run. Installed `~/Library/Audio/Plug-Ins/Components/Context 6.component`. Standalone opened for smoke. Load Context 6, not the Context 5 already on the track.
- Owner: two VSTs (Context 5 and Context 6). Context 5 was leftover so Live would load a new identity. Deleted `Context 5.component` and `Context 5.vst3`. Only Context 6 remains on disk.
- Owner could not find the hidden Prompt Gate. Added a header **Prompts** button that opens SYSTEM / RULES / NEGATIVE. Shipped **Context 8** (`Ctx8`) and removed Context 6/7 from the plug-in folders so Live lists one host.
- Owner still did not see Library on Context 6: `resized()` placed Library next to Run then overwrote it onto the clipped bottom row. Context 8 docks **Sample Library** as a permanent pane and keeps a header Library button. Only Context 8 remains in Plug-Ins. Standalone opened.
- Owner: no special or foreign characters in the plugin UI. Replaced em dashes, middle dots, ellipsis, and curly quotes with ASCII. Display path sanitizes sidecar/file names via `AsciiUi.h`. Shipped **Context 9** and removed Context 5-8 from Plug-Ins.
- Owner: library folder set but search did not list candidates. Cause: sidecar/CLAP-only ranking plus filename-token-only hits, and the table sat in leftover height. Context 9 lists every audio file locally on Search/All, reserved 220px pane, preview on row select via audition buffer, drag via `performExternalDragDropOfFiles` plus **Drag to Live**. Sidecar `/search` is quick filename/path rank and still lists unmatched files. Tests: Phase4 + clap mock OK.
- Owner: fold Library until the header button opens it; put the reference field at the top. Context 10 starts with the sample pane hidden. Library toggles open/close and resizes the editor. Reference row is first: path field, Choose, drop, or Use as reference from the library.
- Owner: library and status under Audition were clipped; buttons did nothing (zero-height hit targets under a short Live window). Context 10 puts the page in a Viewport, reserves Audition/Apply/status, gives the library table its own scrollbars, and leaves buttons enabled during generate (status says Already generating). Older Context 9 removed from Plug-Ins.
- Owner: drag output waveform into the DAW and onto Reference. mouseDown no longer starts an OS drag. Drag outside the plugin exports a stable WAV; drop on the Reference row copies the clip into Context/Plugin/Reference. Shipped Context 11; removed Context 10.
- Owner: rotate to the next music gen tool. `POST /rotate` advanced from `stable_audio_open` to **audioldm2** (index 3 of 7). Health: `id: audioldm2`, label `AudioLDM 2 (experiment only)`. Persist: `~/Library/Application Support/Context/generation-rotate.json`. `_try_model` still returns `audioldm2_experiment_weights_missing`; adapters keep it out of the `/synthesize` commercial path. No stand-in WAV. Next rotate is Suno (`SUNO_API_KEY`).
- Owner: wire AudioLDM 2, no stub. Rotate/compose now loads `AudioLDM2Pipeline` + `cvssp/audioldm2-music` (diffusers 0.40.0, MPS float32, attention slicing, default 80 steps). `GPT2Model` on transformers 5 lacked `_update_model_kwargs_for_generation`; patched after load. Prompts stay under CLAP/T5 limits via `suggestion_block`. Honest errors only. `/synthesize` still `blocked_backend`. Smoke: 128044-byte WAV, backend `audioldm2-music`. Tests: `test_generation_rotate` + `test_blocks_audioldm2` OK. Weights: Application Support `Context/models/models--cvssp--audioldm2-music`.
- Owner: output taking too long to load. Cause: Magenta (up to 180s) and CLAP library embed ran before the plugin got the WAV; AudioLDM 2 was 80 steps and Hub re-fetch. Compose now writes the neural WAV first, skips Magenta unless pedalboard, filename-only search on compose, 24 steps, `local_files_only` when cached, sidecar warms the current pipe after listen. Tests: generation rotate + heavy compose + phase4 block OK.
- Owner: show ETA and preview while generating (Suno-style). Sidecar `GET /progress` reports step/ETA. AudioLDM 2 decodes intermediate latents to `Plugin/.preview/live.wav` every 4 steps. Context 12 polls that during compose, updates the status line, loads the waveform, and starts Audition playback. Context 11 removed from Plug-Ins. Tests: `test_progress` + `test_get_progress` OK.
- Owner: Audition was silent and Apply did not keep the clip. AudioLDM 2 was writing all-zero WAVs (MPS VAE/vocoder during the step callback). Decode now runs on CPU from latents; silent output is an error, not a keepable file. Apply copies the loaded clip (including `.preview/live.wav`) into `Context/Plugin` without regenerating. Stop no longer deletes the clip. Shipped **Context 13** (`Ct13`); Context 12 removed from Plug-Ins. Smoke: 8s 16 kHz WAV, RMS 0.19, peak 0.89, preview also audible. Tests: generation rotate + compose + phase2-6 OK.
- Owner: output was the same for every prompt, and preview would not stop while generating. Apply was always promoting the last clip. Progress ticks also restarted playback after Stop. Context 14: typed text leads the AudioLDM 2 prompt, stable per-prompt seed, guidance 6.0; Stop clears play-after so preview stays stopped; Apply keeps only when the prompt still matches. Context 13 removed. Smoke: "soft Rhodes chords" vs "heavy drum break" wrote different WAVs. Tests: generation rotate + prompt policy OK.
