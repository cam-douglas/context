# 2026-08-29 continuation

- Owner: rotate the main music generation tool each time they say "rotate", to test performance. Built `generation.py` roster (pedalboard → musicGen → Stable Audio Open → AudioLDM 2 → Suno → Udio → ElevenLabs). First rotate is **musicgen**. Compose records seconds in `~/Library/Application Support/Context/generation-perf.jsonl`. Missing weights/keys fall back to pedalboard and log the miss.
- Installed `torch` 2.13.0 and `transformers` 5.16.1 into `sidecar/.venv`. Restarted sidecar on 127.0.0.1:8765 so MusicGen can import. First Apply still downloads `facebook/musicgen-small` into the model cache.
- Owner: rotate did not change the heard file. Cause: Apply wrote local DropWriter first and copied to Desktop; sidecar compose was ignored. Fix: Apply/Audition wait on `/compose` with `dest_dir` = `~/Library/Application Support/Context/Dumps` only. Filenames are `{generator}-{slug}.wav`. Audition plays the last dump through processBlock. MusicGen probe wrote 647724-byte WAV in 23.9s with no fallback. Shipped **Context 4** and removed Context 3.
- Owner: asked bars were ignored and style vocab was too small. Cause: MusicGen used a fixed 256 tokens (~5s) and compose only knew ~14 styles. Fix: duration = bars * 4 * 60 / bpm (1–64 bars); MusicGen tokens/chunks + trim-to-length. Shipped merged genre index from Every Noise at Once (5453) + MusicBrainz (2192) = 6798 names in `sidecar/src/context_sidecar/data/genre_index.json`. Longest-match first. Refresh: `python sidecar/scripts/refresh-genre-index.py`. GET `/genres?q=`. No plugin rescan needed.
- Owner asked about musicmap.info for genealogy. Their Carta is copyrighted (all rights reserved; no public dataset/API; store terms forbid copying/derivatives). Did not scrape or vendor it. Licensed path would be asking Kwinten Crauwels. Shipped open substitute: Wikidata music-genre graph (CC0) — 4847 nodes with parents, influences, year — in `genre_genealogy.json`. Compose/MusicGen prompts now include lineage (e.g. shoegaze → alternative rock, 1987). GET `/genres?q=shoegaze` returns lineage. Refresh: `python sidecar/scripts/refresh-genre-genealogy.py`.
- Owner 2026-08-30: cannot rewrite a Live Set from a VST. Shipped **Context 5** with an in-plugin waveform you drag into the DAW (`performExternalDragDropOfFiles`). Permanent WAV/MIDI: `~/Library/Application Support/Context/Plugin` (not Documents). Audition writes `Plugin/.audition` and deletes on Stop / editor close unless Apply promotes. Drop a reference audio file onto the device for the next compose. Dials are Reverence, Abstraction, Amount, Wet — they change note density, pedalboard FX, MusicGen prompt text, and dry/wet mix. Rotated generator to **stable_audio_open** (not installed; falls back to pedalboard). Removed Context 4 bundles. Sidecar restarted via `sidecar/scripts/run-sidecar.sh`.
